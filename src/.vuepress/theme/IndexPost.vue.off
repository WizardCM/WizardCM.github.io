<template>
  <div>
    {{ $pagination.pages }}
    <page-card
      v-for="page in $pagination.pages"
      :key="page.key"
      :page="page"
      orientation="horizontal"
    />
  </div>
</template>